"use.strict";

/*EJERCICIO 9
Arrow functions everywhere
Vamos a rehacer alguno de los ejercicios anteriores con funciones flecha. ¡A lo loco!*/

const getElement = getEl(a) => document.querySelector(a);


const pairOddNumber = (pairodd) => return (pairodd) % 2;
  

const valorResto = pairodd(number);
if (valorResto !== 0) {
  console.log("Impar");
} else {
  console.log("Par");
}
