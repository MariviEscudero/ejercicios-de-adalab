"use strict";
let nombre = "Clara";

if (nombre === "Clara" && "Marivi") {
  console.log(`Bienvenida ${nombre}`);
} else {
  console.log(
    "Lo siento, pero el usuario que has introducido no esta registrado"
  );
}
nombre = "Maricarmen";
if (nombre === "Clara" && "Marivi") {
  console.log(`Bienvenida ${nombre}`);
} else {
  console.log(
    "Lo siento, pero el usuario que has introducido no esta registrado"
  );
}
